# Dipendenze OPZIONALI per provider aggiuntivi (v1.2+).
# NON necessarie per l'MVP (OpenAI Realtime + demo). Installa solo quello che
# ti serve; i moduli relativi usano import lazy e restituiscono un errore
# leggibile se il pacchetto manca.

# Cattura audio dell'uscita di sistema (WASAPI loopback su Windows): permette di
# tradurre l'audio riprodotto dal PC (es. un video YouTube). Senza questo, il
# menu "Ingresso audio" mostra solo microfoni/line-in.
soundcard>=0.4.3

# Provider di traduzione cloud
#   DeepL usa solo httpx (già in requirements.txt): nessuna dipendenza extra.

# Provider vocali cloud (STT)
azure-cognitiveservices-speech>=1.34   # AzureSpeechProvider
google-cloud-speech>=2.21              # GoogleSpeechProvider

# Provider locali (v1.3) — riconoscimento vocale e traduzione offline. Richiedono
# molto spazio su disco (modelli scaricati alla prima esecuzione) e idealmente
# una GPU NVIDIA; su CPU la latenza può essere alta.
faster-whisper>=1.0    # FasterWhisperSpeechProvider (STT locale)
transformers>=4.40     # LocalMarianTranslationProvider (traduzione locale)
torch>=2.2             # backend per transformers
sentencepiece>=0.2     # tokenizer dei modelli MarianMT (Helsinki-NLP/opus-mt-*)
